version: 1
backend:
  phases:
    build:
      commands:
        - pip install -r requirements.txt
        - echo "Build completed on $(date)"
  artifacts:
    baseDirectory: /
    files:
      - '**/*'
  cache:
    paths: []
frontend:
  phases:
    build:
      commands:
        - echo "No frontend build needed"
  artifacts:
    baseDirectory: /
    files:
      - '**/*'
  cache:
    paths: []